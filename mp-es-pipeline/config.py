"""
Пути и замороженные параметры профиля. Всё, что влияет на числа, живёт здесь.
Любая правка параметров ниже обнуляет результаты тестов, которые на них опираются:
после правки пишется строка в журнал решений README и тесты пересчитываются.
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parent
# Минутки не дублируются: читаются из COT-пайплайна (Databento GLBX.MDP3, ES.v.0, ohlcv-1m)
ES_DIR = ROOT.parent / "cot-es-pipeline" / "data" / "es"
DERIVED = ROOT / "data" / "derived"

TZ = "America/New_York"
TICK = 0.25

# Сессии (время New York). ETH (ночь) начинается накануне в 18:00, с открытием Globex.
# 16:00–17:00 не входит ни в дневной профиль, ни в ночь, но входит в недельный и месячный
RTH_START = "09:30"
RTH_END = "16:00"  # не включая: последний минутный бар 15:59
ETH_START = "18:00"
FULL_DAY_BARS = 390
# Праздничная сессия Globex (NYSE закрыта): RTH-бары обрываются до 13:00.
# Настоящий укороченный день (канун праздника) закрывается в 13:15
HOLIDAY_LAST_BAR = "13:10"

# Периоды блоков (TPO)
PERIOD_MIN = 30            # дневной профиль: 30 минут, буквы A…M, только RTH
LETTERS = "ABCDEFGHIJKLM"
WEEK_BLOCK_MIN = 240       # недельный: 4 часа от 18:00 (18–22, 22–02, 02–06, 06–10, 10–14, 14–17)
                           # месячный: 1 блок = сутки Globex 18:00 → 17:00

# Профиль
VA_SHARE = 0.68
VA_METHOD = "two_row"      # классический CBOT: сравниваются суммы двух строк сверху и снизу
IB_PERIODS = 2             # A + B
TAIL_MIN_ROWS = 2          # excess / single prints = не меньше 2 блоков; excess от последнего блока не считается
POOR_MIN_TPO = 2           # poor high / low = не меньше 2 отметок на крайней строке

# Размер блока (высота строки профиля). Каждый профиль считается в обоих режимах,
# тесты показывают результаты рядом
ROW_MODES = ("fixed", "adaptive")
ROW_FIXED = {"day": 2.0, "week": 4.0, "month": 8.0}  # настройки терминала на 2026 год
# adaptive: дневной блок = медиана диапазона RTH за 20 прошлых дней / 25, округление до сетки,
# пересчёт по понедельникам. Сдвиг на 1 ступень применяется, если держится 2 понедельника подряд,
# на 2+ ступени сразу. Недельный = дневной × 2, месячный = дневной первой недели месяца × 4
ROW_LOOKBACK_DAYS = 20
ROW_DAY_DIVISOR = 25
ROW_LADDER = (0.25, 0.5, 0.75, 1.0, 1.5, 2.0, 2.5, 3.0, 4.0, 5.0, 6.0, 8.0)
ROW_CONFIRM_WEEKS = 2
ROW_JUMP_STEPS = 2
ROW_MULT = {"week": 2, "month": 4}

# Дневные композиты (mp/composite.py). Тумблер в тестах: опора = вчерашний день или композит.
# Недельные и месячные профили не склеиваются
COMPOSITE_MODES = ("off", "on")
COMP_MIN_OVERLAP = 0.50       # доля VA нового дня внутри VA композита, строго больше
COMP_POC_POS = (0.25, 0.75)   # POC композита в средней половине диапазона
COMP_PEAK_RATIO = 0.80        # второй пик ≥ 80 % главного …
COMP_TROUGH_RATIO = 0.60      # … и провал между ними ≤ 60 % второго пика → двойное распределение
COMP_PEAK_MIN_GAP = 3         # пики дальше 3 строк друг от друга (сглаживание по 3 строкам)
COMP_VA_SYM = 1 / 3           # короткая сторона VA от POC ≥ 1/3 длинной
# Трендовый день и trend day с двойным распределением не добавляются в композит.
# Включится, когда будут заморожены правила типов дня (шаг классификации)
COMP_EXCLUDE_DAY_TYPES = ("trend", "double_distribution_trend")
